// import Swal from 'sweetalert2'
import {Producto,Comprar} from '../Productos/Producto';
export default function Carrito() {

    // const listaCompar = async() =>{
    //     try {
    //         await Comprar()


    //     } catch (error) {
    //         throw error
    //     }
    // }

    return (
        <div className="container">
            {/* {carrito.map((item, i)=>{
                
            <Producto key={i} item={item}/>
            })} */}
            {/* <Comprar/> */}
        </div>
    )
}
